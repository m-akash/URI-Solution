#include <stdio.h>
 
int main() {
 
    char name[30];
    float salary;
    double selling,TOTAL;
    gets(name);
    scanf("%f %lf",&salary,&selling);
    
    TOTAL=salary+(selling*15)/100;
    printf("TOTAL = R$ %.2lf\n",TOTAL);
 
    return 0;
}
